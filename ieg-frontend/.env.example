# IEG Frontend — Environment Variables
# Copy to .env

# Backend API URL (proxied via Vite in dev — set to full URL for production)
VITE_API_URL=http://localhost:5000/api/v1
VITE_WS_URL=http://localhost:5000
